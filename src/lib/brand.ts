// Central brand config. The platform name is being workshopped pre-pilot, so
// every visible reference to the brand reads from here. When the final name is
// chosen, change BRAND (and the optional fields) in this one place.
//
// NOTE: "Preened" is a working PLACEHOLDER — not the final name.
export const BRAND = "Preened";

// Short value-prop shown under the wordmark / in the hero.
export const BRAND_TAGLINE = "Book local self-care pros who come to you";

// Prefix for human-facing booking references (e.g. PRN-AB12CD).
export const BOOKING_REF_PREFIX = "PRN";
